<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.M500Hardware" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.M500Hardware.Translucent" parent="Theme.M500Hardware">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- OS lockscreen theme: fullscreen, no title, transparent bars. Uses a curtain-drop window
         animation so the lockscreen falls from the top over the launcher (an integral system layer)
         instead of popping in like an app. -->
    <style name="Theme.MikuLockscreen" parent="android:Theme.Material.NoActionBar.Fullscreen">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">#040D12</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowAnimationStyle">@style/MikuLockscreenCurtainAnim</item>
        <item name="android:windowSplashScreenAnimationDuration">0</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- Curtain drop: the lockscreen slides down from the top over whatever's behind it (the
         launcher / current app), then retracts up on unlock. Makes it read as a system shade. -->
    <style name="MikuLockscreenCurtainAnim">
        <item name="android:windowEnterAnimation">@anim/lockscreen_curtain_down</item>
        <item name="android:windowExitAnimation">@anim/lockscreen_curtain_up</item>
    </style>
</resources>
