<?xml version="1.0" encoding="utf-8"?>
<!-- Miku BPM tap-tempo widget: round tap orb + computed tempo + session tap count. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_tapper_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:gravity="center"
    android:background="@drawable/miku_badge_widget_bg"
    android:padding="6dp">

    <Button
        android:id="@+id/widget_tapper_btn"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:background="@drawable/miku_tap_orb_bg"
        android:text="♥"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:textStyle="bold"
        android:padding="0dp" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="6dp"
        android:orientation="vertical"
        android:gravity="center">

        <TextView
            android:id="@+id/widget_tapper_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="— BPM"
            android:textColor="#FF4FA3"
            android:textSize="12sp"
            android:textStyle="bold"
            android:maxLines="1" />

        <TextView
            android:id="@+id/widget_tapper_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="TAP IN"
            android:textColor="#9FF3EC"
            android:textSize="8sp"
            android:maxLines="1" />
    </LinearLayout>
</LinearLayout>
